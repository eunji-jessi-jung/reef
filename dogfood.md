# Reef Dogfooding Log

## 2026-04-10

- **--:--** ~~[friction] reef:init treats parent directories as sources instead of discovering the actual git repos inside them.~~ **RESOLVED** — init Step 2 now checks for .git/ and auto-discovers repos one level down if the given path is not itself a git repo.
- **--:--** ~~[friction] User can't understand what to do during the repo registry step of reef:init.~~ **RESOLVED** — registry step removed from init entirely.
- **--:--** ~~[friction] Repo registry asks too much from the user upfront.~~ **RESOLVED** — registry removed; init is now 3 questions then full automation.
- **--:--** ~~[friction] reef:init should not ask "want to set up registry files?" after indexing.~~ **RESOLVED** — no registry prompt; init flows straight from index to question generation to snorkel.
- **--:--** ~~[friction] reef:init doesn't explain why feeding existing docs matters.~~ **RESOLVED** — init Step 3 now mentions sources/raw/ and the value of existing docs after scaffolding.
- **--:--** ~~[idea] Create a /reef:feed skill for adding raw documents to an existing reef after init.~~ **RESOLVED** — /reef:feed skill created. Supports file paths, directory scanning, or manual placement.
- **--:--** ~~[idea] reef:init should tell the user they can drop raw docs into sources/raw/.~~ **RESOLVED** — init mentions sources/raw/ after scaffolding and points to /reef:feed.
- **--:--** ~~[friction] Question bank step during init feels overwhelming.~~ **RESOLVED** — init now auto-generates discovery questions from structural scan + understanding template. No user input required.
- **--:--** ~~[bug] reef.py `snapshot` and `rebuild-map` commands crash with `TypeError: unhashable type: 'dict'`.~~ **RESOLVED** — fixed in reef.py; all three locations now handle dict source entries.
- **--:--** ~~[idea] After /reef:init completes, /reef:snorkel should be automatically triggered.~~ **RESOLVED** — init Step 6 now auto-triggers snorkel.
- **--:--** ~~[friction] Reef can't go deeper without thoughtfully curated discovery questions.~~ **RESOLVED** — snorkel is now question-guided; reads questions.json and uses it to steer investigation.
- **--:--** ~~[idea] Show health graphics after /init or /snorkel completes.~~ **RESOLVED** — snorkel Step 5 now runs lint+diff and renders a compact health table before the summary.
- **--:--** ~~[friction] reef:init should tell the user they can drop reference docs into sources/raw/.~~ **RESOLVED** — covered by init Step 3 sources/raw/ mention and /reef:feed skill.
- **--:--** ~~[idea] reef:init should offer to scan a user-provided directory for relevant docs.~~ **RESOLVED** — /reef:feed handles this: accepts a directory path, scans for .md/.pdf/.docx/.txt/.json/.yaml, copies into sources/raw/.
- **--:--** ~~[friction] Remove the question bank step from /init entirely.~~ **RESOLVED** — init auto-generates questions from structural scan (Step 5) and goes straight into snorkel (Step 6). No user input between index and snorkel.
- **--:--** ~~[idea] reef:init should open with a brief, catchy introduction of what Reef is and what it does — make it instinctive so the user immediately gets the concept. Include a text-based graphic showing the reef concept visually.~~ **RESOLVED** — Step 0 added: ASCII concept graphic, model recommendation (Opus), what-we-will-do summary, time/token estimate (~10-15 min, 50-80k tokens), and post-init next steps.
- **--:--** ~~[bug] reef:init accepts plain folders as sources without verifying .git exists at root. Only directories with .git/ should be treated as valid source codebases. Folders without .git should be rejected or the user should be warned.~~ **RESOLVED** — Step 2 now rejects paths with no .git at root or one level down. Suggests /reef:feed for doc folders. Blocks init if all paths rejected.
- **11:50** ~~[idea] reef:init should ask "Could you give a brief introduction of what this reef is for?" after the name question. Front-loads domain context (service names, ecosystem, product area) so service grouping and question generation are more accurate instead of relying solely on code scanning inference.~~ **RESOLVED** — Step 2 now asks "what is it for?" after name, before location. Saved to project.json description field.
- **21:50** [bug] After snorkel completes, the health report starts rendering but the Unicode box-drawing graphics never appear. The snorkel skill specifies "Render a compact health summary using the /reef:health report format (text table with Unicode box-drawing)" but the output gets cut off or skipped. The graphics are key — they give the user an immediate visual sense of coverage and freshness.
- **22:05** [bug] /reef:obsidian fails to open a reef that hasn't been registered as an Obsidian vault yet. The `obsidian://open?path=` URI only works for already-registered vaults. Tried `obsidian://new-vault?path=` and `open -a Obsidian <path>` — neither registers a new vault. There appears to be no CLI or URI to register a folder as a vault for the first time. The skill should fall back to `open -n -a "Obsidian"` (opens Obsidian) and tell the user to add the vault manually, rather than silently failing or opening Finder.
- **22:10** [bug] Snorkel-generated artifacts have incomplete relates_to links. For example, sys-cdm and sch-cdm-core are not linked to each other, even though the schema clearly belongs to that system. This means the Obsidian graph view shows disconnected nodes instead of a connected knowledge graph. The snorkel skill (or a post-generation pass) needs to ensure bidirectional relationships — if a SCH- artifact is generated for a service, it should relate back to the SYS- artifact and vice versa.
